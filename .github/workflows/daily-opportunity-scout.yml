name: Daily Opportunity Scout

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Actions smoke test
        run: |
          echo "GitHub Actions runner works"
          echo "Commit: $GITHUB_SHA"
